dependencies: 
  - recipe: flink::install
    local:
      - hops::install
  - recipe: flink::jobmanager
    local:
      - flink::install
    global:  
      - hops::dn
  - recipe: flink::taskmanager
    local:
      - flink::install
    global:  
      - flink::jobmanager
  - recipe: flink::wordcount
    local:
      - flink::install
    global:  
      - flink::taskmanager
